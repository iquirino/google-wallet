import { FieldSelector } from "./FieldSelector.js"
export type BarcodeSectionDetail = {
	/**
	 * A reference to an existing text-based or image field to display.
	 */
	fieldSelector?: FieldSelector;
}
